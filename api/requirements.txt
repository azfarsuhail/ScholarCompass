# Loose constraints here; requirements.lock.txt holds the resolved pins that
# the Docker image installs. Every dep below is pure-python or a small C ext --
# nothing torch-shaped, because the container ceiling is 512MB.

# --- core ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
httpx>=0.28
python-multipart>=0.0.20
itsdangerous>=2.2
# Anonymous abuse control. Pure python, ~800KB with its `limits` dep, and the
# default storage is an in-process dict -- no Redis to fit in the container.
slowapi>=0.1.9

# --- OCR: tesseract binary + thin wrappers. No torch. ---
pytesseract>=0.3.13
pillow>=11
pypdfium2>=4.30

# --- RAG retrieval is Postgres full-text search on Neon. No embedding vendor:
#     Groq has no embeddings endpoint, and a local embedder (torch) would blow
#     the container on its own.
#     ponytail: FTS + LLM rerank. Add pgvector + a hosted embedder only if
#     recall measurably falls short.

# --- LLM: Groq LPU inference ---
groq>=0.13

# --- tests ---
pytest>=8.3
pytest-asyncio>=0.24
respx>=0.22

# playwright is NOT here on purpose -- it would blow the 512MB API container.
# See requirements-scraper.txt, installed only by Dockerfile.ingest and the
# weekly GitHub Actions scraper.
